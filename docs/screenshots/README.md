# Screenshot placeholders for README

Capture these screens after `pnpm dev` with demo mode:

1. `dashboard.png` — `/dashboard`
2. `transactions.png` — `/transactions`
3. `onboarding.png` — `/onboarding`

Recommended size: 1440×900 WebP or PNG.
